/**
 * Toán tử so sánh 2 - Comparison operators 2
 * ===
 * !==
 */

var a = 1;
var b = '1';

console.log(a == b);// true, so sánh value
console.log(a === b);// false, so sánh value, datatype

console.log(a != b);// false, so sánh value
console.log(a !== b);// false, so sánh value, datatype

